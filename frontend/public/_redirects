# Cloudflare Pages only (the Caddy image has its own try_files rule).
# Pages ignores this rule while a 404.html exists; the Deploy workflow removes
# 404.html (`npm run generate:pages`), and this file stays as belt-and-braces.
/*  /index.html  200
