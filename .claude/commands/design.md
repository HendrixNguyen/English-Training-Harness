---
description: Design a UI idea or frontend bug into harness/designs/<slug>.md (designer role)
---

Spawn the `harness-designer` agent on the idea path given in `$ARGUMENTS` (a second argument may be the plan path to attach the design to). If no argument is given, pick the first `selected` or `proposed` idea whose Expected output touches `frontend/` and say which one you chose.
