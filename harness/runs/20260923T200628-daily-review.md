# Daily review — 2026-09-23 (scheduled, unattended)

## Outcome
Every plan executed on 2026-09-23 already has a review. The 12 reviews filed today are 2 `pass`, 10 `pass-with-bugs`
and 0 `fail`. There are no
unresolved blockers (`cli.py blockers` exits 0). Daily PR #8 (`harness/daily-2026-09-23`, 3 fixes) is open, and both CI
runs on it are green (backend-unit, backend-integration, frontend, harness-tooling). It is ready for the owner to merge.

`next --stage review` still returns four plans, but they are false positives. All four are 2026-09-22 amend plans that
are `merged: true` and were reviewed under their parent plan. Filed as a tooling bug (below). No re-review was done.

## Independent re-verification (daily branch `origin/harness/daily-2026-09-23`, temp worktree)
```
go build ./... && go vet ./...                                   # ok
go test -timeout 300s ./...                                      # 10/10 packages ok
go test -race -count=3 ./internal/pet/... ./internal/quests/... ./internal/notify/...   # ok, no races
gofmt -l .   # internal/quests/handler_test.go, internal/quests/repo.go (already filed: gofmt-l-has-been-failing-…)
```
The frontend was not re-run locally. CI's `frontend` job is green on both PR #8 runs.

## Bookkeeping drift (owner action, not changed here)
- `2026-09-23-notify-web-push-subscriptions-and-delayed-reminder-queue.md` and
  `2026-09-23-push-subscription-endpoint-is-an-unvalidated-user-supplied-u.md` say `merged: false`, but PR #4 is
  merged and both head commits (`538a8bf`, `b85189a` with the SSRF guard) are ancestors of `origin/main`. Run
  `/harness merge` for each, or `cli.py set … merged=true`, so STATE.md stops listing them as unmerged.

## Bugs filed today
- `harness/ideas/_inbox/next-stage-review-re-queues-merged-amend-plans-whose-review-.md` [low]: the review queue
  counts only reviews whose `plan:` matches exactly.

## For tomorrow's evaluation (ranked on user impact)
1. **CORS** — `the-api-sends-no-cors-headers-…` [high, inbox]. The deployed PWA reaches zero endpoints. Bundle it
   with the draft plan `2026-09-23-main-go-installs-a-signal-handler-…` [high, awaiting `/approve`]; both touch `cmd/api/main.go`.
2. **Plaintext refresh token** — `google-refresh-token-is-stored-in-plaintext-…` [high, selected]. Backend spec §7
   requires AES-256-GCM.
3. **Pet correctness** — `ontargetmet-s-read-then-write-erases-a-concurrent-sweep-s-mi…`,
   `get-quests-daily-still-reads-is-target-met-from-the-volatile…` and
   `a-multi-day-sweep-outage-collapses-every-missed-day-into-one…` [medium]. These are what remains of today's pet-verdict work.
4. **Happy-path UX** — `a-cleared-reminder-time-field-dead-ends-onboarding-…`,
   `nothing-tells-the-pwa-to-flatten-pushsubscription-tojson-…` and `every-google-409-now-becomes-500-…` [medium].
5. **CI hardening** — `ci-never-runs-gofmt-…` and `ci-never-runs-go-test-race-…` [medium, selected]. Today gofmt is
   red locally and nothing in CI catches it.

The inbox holds 22 proposed reviewer bugs that need evaluation (`/evaluate`).
